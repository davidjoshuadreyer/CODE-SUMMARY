// Original retrieval and code-tracing questions tied to the imported tutorial sequence.
const questions=[];
function q(topic,prompt,options,correct,explanation){questions.push({id:'c'+(questions.length+1),topic:topic-1,prompt,options,correct,explanation});}
q(1,'What is the value of `3 / 2` in C++ when both operands are integers?',['`1`','`1.5`','`2`','It does not compile'],0,'Integer division discards the fractional part. Use a floating-point operand such as `3.0 / 2` to get `1.5`.');
q(1,'If a circle’s radius doubles, how does `PI * radius * radius` change?',['It doubles','It quadruples','It stays the same','It increases by PI'],1,'Both factors of radius double, so their product becomes four times as large.');
q(2,'What happens to `cin >> temperature` when the next token is `q` and temperature is a double?',['It stores zero and succeeds','It automatically skips q','Extraction fails and sets the stream’s failure state','It always throws an exception'],2,'With the usual stream settings, nonnumeric input sets failbit. Test the stream result; the tutorial exits its input loop on this failure.');
q(2,'Which condition detects a temperature outside the inclusive range 0 to 40?',['`t < 0 && t > 40`','`t <= 0 || t >= 40`','`t > 0 && t < 40`','`t < 0 || t > 40`'],3,'A value is outside if it is below the lower bound OR above the upper bound. The endpoints are allowed.');
q(3,'Which parameter lets a function change the caller’s integer directly?',['`int value`','`int& value`','`const int value`','`double value`'],1,'A reference parameter aliases the caller’s object. A plain value parameter receives a copy.');
q(3,'What is special about a local variable declared `static`?',['It is recreated with its initializer on every call','It is visible from every source file','It retains its value between function calls','It can never be changed'],2,'A static local has static storage duration. Its initialized value and subsequent changes persist across calls.');
q(4,'Which loop visits each element of `int data[3]` exactly once?',['`for (int i=0; i<=3; ++i)`','`for (int i=1; i<=3; ++i)`','`for (int i=0; i<3; ++i)`','`for (int i=0; i<2; ++i)`'],2,'The valid indices are 0, 1, and 2. Accessing index 3 is out of bounds.');
q(4,'For `string word = "banana"`, what does `word.substr(1, 3)` return?',['`"ban"`','`"ana"`','`"nan"`','`"anana"`'],1,'The first argument is the zero-based start index, and the second is the number of characters. Indices 1 through 3 spell ana.');
q(5,'After `int value=5; int* p=&value; *p=9;`, what is value?',['`5`','`9`','The address of p','Undefined because p is local'],1,'The pointer holds value’s address, and dereferencing it writes to the original integer.');
q(5,'Which operation correctly releases an array allocated by `new double[5]`?',['`delete readings;`','`free(readings);`','`delete[] readings;`','`readings = nullptr;` alone'],2,'Array new must be paired with array delete. Setting a pointer to nullptr does not free the allocation.');
q(6,'A function is declared in a header but its implementation is not linked. What failure usually results when it is called?',['A missing-definition linker error','A syntax error in every include','The function returns zero','The function is automatically generated'],0,'A declaration describes the interface. The linker still needs a matching definition from a compiled source file.');
q(6,'Why do the geometry tests compare doubles with a tolerance?',['Doubles contain only integers','Floating-point calculations can introduce rounding error','The compiler cannot compare doubles','A tolerance makes every answer pass'],1,'Use an appropriately small tolerance for approximate numerical results, rather than demanding exact equality after floating-point operations.');
q(7,'Which member initializes a newly created object?',['The destructor','The assignment operator','The constructor','The namespace'],2,'A constructor initializes an object. Assignment updates an object that already exists, and a destructor runs when its lifetime ends.');
q(7,'A class has a static integer `liveCount`. How many copies of that member are shared by its objects?',['One for each object','One shared class member','Exactly two','None until a destructor runs'],1,'A static data member is shared rather than stored separately inside each instance. Its definition must follow the language’s declaration/definition rules.');
q(8,'A base pointer refers to a derived object and calls an overridden virtual method. Which implementation runs?',['Always the base implementation','The derived override','Whichever function was compiled first','Neither; virtual calls are illegal'],1,'Virtual dispatch selects the override for the object’s dynamic type when called through a base pointer or reference.');
q(8,'Why should a polymorphic base class support a virtual destructor when objects will be deleted through base pointers?',['To make all fields public','To prevent inheritance','To ensure the derived object is properly destroyed','To make the destructor return a value'],2,'Deleting through a base pointer requires a virtual base destructor for the usual polymorphic ownership pattern. Otherwise the operation can have undefined behavior.');
q(9,'Where do template definitions normally need to be visible?',['Only in an unrelated executable','At the point of instantiation, commonly through a header','Only in a comment','Only after the program runs'],1,'The compiler generally needs the template definition when generating a specialization. Header definitions are the usual approach; explicit instantiation is an alternative.');
q(9,'A function template returns `a > b ? a : b`. What must its chosen type support?',['Only integer arithmetic','An applicable comparison using `>`','A virtual destructor in every case','Conversion to a raw pointer'],1,'Templates are checked for the operations they use when instantiated. They do not make unsupported comparisons valid.');
q(10,'When deleting a linked list’s first node, what should happen before deleting that node?',['Delete every following node without saving links','Save or advance to the next node','Overwrite every value with zero','Delete the traversal pointer twice'],1,'Save the next link while the current node is still alive. Reading current->next after deleting current accesses freed memory.');
q(10,'What does advancing a traversal pointer with `p = p->next` do?',['Deletes the current node','Moves the current node in memory','Changes which node p refers to','Copies the whole list'],2,'Only the pointer value changes. Nodes remain allocated and linked until other code modifies or deletes them.');
q(11,'Push 10, 20, then 30 onto a stack. In what order are they popped?',['10, 20, 30','30, 20, 10','20, 10, 30','The order is unspecified'],1,'A stack is last-in, first-out. A queue would remove these items in arrival order.');
q(11,'What is the difference between `top()` and `pop()` in the tutorial stack?',['Both delete the whole stack','top looks at the top item; pop removes it','top removes; pop only looks','They always return the same value'],1,'Inspection and removal are separate operations. Check the container’s interface for return types and empty-container behavior.');
q(12,'Which loop checks whether each read actually succeeded?',['`while (!input.eof()) { input >> value; }`','`while (input >> value) { /* use value */ }`','`while (true) { /* use stale value */ }`','`while (input.bad()) { input >> value; }`'],1,'A read can discover EOF or invalid data. Testing the extraction itself prevents processing an old value after a failed read.');
q(12,'What does `ios::app` do when opening an output file?',['Erases the file before each write','Reads only binary numbers','Appends writes to the end','Automatically sorts the file'],2,'Append mode preserves existing content and writes at the end. The file still must open successfully.');
q(13,'After an exception is thrown inside a try block, what happens to the remaining statements in that block?',['They always run before catch','They are skipped as control transfers toward a matching handler','They run twice','They become compile-time statements'],1,'Exception propagation skips those statements and unwinds scopes as required until a suitable catch is found.');
q(13,'To recover from invalid keyboard input, what do `clear()` and `ignore()` typically do?',['Both delete the stream','clear resets error flags; ignore discards unwanted input','clear empties the whole file; ignore resets flags','They change integers into strings'],1,'Resetting flags allows further reads, while discarding the bad input prevents reading the same invalid characters again.');
q(14,'Which vector access operation checks bounds and throws for an invalid index?',['`values[i]`','`values.at(i)`','`values.begin()`','`values.capacity()`'],1,'at performs bounds checking and throws out_of_range. The subscript operator does not provide the same check.');
q(14,'Does destroying a `vector<Shape*>` automatically delete the separately allocated shapes?',['Yes, always','Only when the vector is sorted','No; raw pointers do not express owning cleanup','Only when capacity equals size'],2,'The vector destroys its pointer elements, not the pointees. Use an explicit ownership policy or owning smart pointers where appropriate.');
q(15,'Why does a controller preserve the integral accumulator between time steps?',['To remember accumulated error','To erase all prior error','To keep the derivative always zero','To disable feedback'],0,'Integral action accumulates error over time. Resetting it each iteration removes that history.');
q(15,'In the tutorial, why is the first derivative contribution initialized to zero?',['The target is always zero','There is no previous error sample to compare','Derivatives cannot be computed numerically','All controllers ignore derivative action'],1,'The first sample has no earlier error for a finite-difference estimate. Later steps compare current and previous errors.');
q(16,'For positive mass and spring stiffness, which expression gives the undamped angular frequency?',['`k / m`','`sqrt(k / m)`','`m * k`','`sqrt(m / k)`'],1,'Angular frequency is the square root of stiffness divided by mass. In C++, ensure the operands use floating-point arithmetic.');
q(16,'For positive mass and stiffness with positive damping, what does `b*b - 4*m*k == 0` identify?',['Undamped motion','Underdamped motion','Critical damping','Overdamped motion'],2,'The characteristic equation has a repeated real root at critical damping. Negative discriminant is underdamped; positive is overdamped.');
q(17,'How many sample points are needed for N integration intervals?',['N - 1','N','N + 1','2N + 1 in every method'],2,'Each interval lies between adjacent sample points. The first and last endpoints are both included.');
q(17,'What restriction does composite Simpson’s one-third rule place on the number of intervals?',['It must be odd','It must be positive and even','It must be a power of ten','It must equal the polynomial degree'],1,'Simpson’s rule groups adjacent intervals in pairs, using weights 1,4,2,4,...,2,4,1.');
q(18,'Why does tutorial 18 link `CricleX.cpp` as well as including its header?',['Headers cannot contain classes','The source provides definitions required by the linker','Every program must include all .cpp files','The filename automatically creates a main function'],1,'The header declares the interface. The matching implementation must be compiled and linked for the methods used by the tutorial.');
q(18,'Why are the original reference demos compiled as separate executables?',['C++ cannot use more than one source file','The demos may reuse class names or define separate entry points','Headers are forbidden in a project','It avoids compiling anything'],1,'Separate executables isolate conflicting names and entry points. The course launcher chooses which program to execute.');
module.exports={questions};
