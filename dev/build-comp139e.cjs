// Rebuild the hosted lessons and Code Desk from the imported local workspace.
const fs=require('node:fs'),path=require('node:path');
const root=path.resolve(__dirname,'..'),dir=path.join(root,'comp139e'),workspace=path.join(dir,'workspace');
require('./build-comp139e-online.cjs');
const programs=JSON.parse(fs.readFileSync(path.join(dir,'programs.json'),'utf8'));
const {format}=require('../assets/study/code.js');
const escape=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const url=s=>s.split('/').map(encodeURIComponent).join('/');
const read=s=>fs.readFileSync(path.join(workspace,s),'utf8');
const code=s=>`<pre class="source"><code>${escape(s)}</code></pre>`;
const guide=read('tutorials/README.md');
const concepts=Object.fromEntries([...guide.matchAll(/^### (\d+)\. ([^\n]+)\n([\s\S]*?)(?=^### |^## |$(?![\s\S]))/gm)].map(m=>[Number(m[1]),{title:m[2],body:m[3].trim()}]));
const lessons=programs.filter(p=>p.kind==='tutorials').map(p=>{
 const n=Number(p.id.split(':')[1]),source=read(p.source),header=source.match(/^\/\*([\s\S]*?)\*\//)?.[1]||'';
 const notes=header.split('\n').map(s=>s.replace(/^\s*\*\s?/,'').trim()).filter(Boolean);
 return {slug:'lesson-'+String(n).padStart(2,'0'),title:String(n).padStart(2,'0')+' · '+p.title,summary:concepts[n]?.title||p.title,source:p.source,program:p.id,notes,concept:concepts[n],sources:['comp139e/workspace/'+p.source]};
});
const bank={id:'comp139e',testMinutes:45,quizPerTopic:1,testPerTopic:1,lessons,questions:require('./comp139e-content.cjs').questions};
const shell=(title,body,extra='',wide=false)=>`<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${escape(title)} · COMP 139E</title><link rel="stylesheet" href="../engr290/course.css"><link rel="stylesheet" href="../assets/vendor/katex/katex.min.css"><link rel="stylesheet" href="course.css?v=online-1"><script defer src="../engr290/theme.js"></script>${extra}</head><body><a class="skip" href="#main">Skip to content</a><header><a class="brand" href="../index.html#course/comp139e">tesselate<span>.</span></a><nav aria-label="Course navigation"><a href="review.html">Lessons</a><a href="desk.html">Code Desk</a><a href="labs.html">Labs</a><a href="quiz.html">Quiz</a><a href="exam.html">Practice test</a><button id="theme" type="button" aria-label="Toggle dark appearance">◐</button></nav></header><main id="main"${wide?' class="wide"':''}><p class="eyebrow">COMP 139E / FALL 2026</p>${body}</main><footer><a href="../index.html#course/comp139e">← COMP 139E workspace</a><p>C++ and MATLAB study material from your existing course setup.</p></footer></body></html>`;
(async()=>{
 const {marked}=await import('marked');
 function markdown(text,base){
  return marked.parse(text,{walkTokens(token){if(['link','image'].includes(token.type)&&!/^https?:|^#/.test(token.href)){const target=path.posix.normalize(path.posix.join(base,decodeURIComponent(token.href)));token.href='workspace/'+url(target);}}});
 }
 const extras={15:String.raw`For a simple sampled PID model, \[u_n=K_p e_n+K_i I_n+K_d\frac{e_n-e_{n-1}}{\Delta t}.\] Match the tutorial's initialization and time-step convention; use the Lab 5 handout for its specific implementation.`,16:String.raw`The undamped angular frequency is \[\omega=\sqrt{\frac{k}{m}}.\] For the damped system, compare \(b^2-4mk\) with zero.`,17:String.raw`With an even positive number of intervals, \[h=\frac{b-a}{N},\qquad S=\frac{h}{3}\left[f_0+f_N+4\sum_{\text{odd }i}f_i+2\sum_{\substack{\text{even }i\\0<i<N}}f_i\right].\] The code keeps C++ division operators; the mathematical formula uses stacked fractions.`};
 for(let i=0;i<lessons.length;i++){
  const l=lessons[i],p=programs.find(p=>p.id===l.program),concept=l.concept;
  if(!concept)throw Error('Missing concept for lesson '+(i+1));
  fs.writeFileSync(path.join(dir,l.slug+'.html'),shell(l.title,`<a href="review.html">← All lessons</a><h1>${escape(l.title)}</h1><p class="lead">${escape(l.summary)}</p><section><h2>Learn the idea</h2><div class="prose">${markdown(concept.body,'tutorials')}</div>${extras[i+1]?`<div class="prose">${format(extras[i+1])}</div>`:''}</section><section class="worked"><h2>Predict, run, change</h2><ul>${l.notes.filter(n=>!/^LESSON|^Connects|^Run:/.test(n)).map(n=>`<li>${escape(n)}</li>`).join('')}</ul><p>Read the expected result, trace the statements, then try the change suggested above. Compile and run your edited C++ directly in the online Code Desk.</p><div class="actions"><a class="button primary" href="desk.html#${encodeURIComponent(l.program)}">Open in Code Desk</a><a class="button" href="setup.html">Run on your computer</a></div></section><section><h2>The commented example</h2><p><a href="workspace/${url(l.source)}" download>Download ${escape(path.posix.basename(l.source))}</a></p>${code(read(l.source))}${p.files.length>1?`<details><summary>Related headers and source files</summary><ul>${p.files.filter(f=>f!==l.source).map(f=>`<li><a href="workspace/${url(f)}">${escape(f)}</a></li>`).join('')}</ul></details>`:''}</section><div class="actions"><a class="button primary" href="quiz.html?topic=${i}">Check your understanding</a>${lessons[i+1]?`<a class="button" href="${lessons[i+1].slug}.html">Next lesson →</a>`:''}</div>`));
 }
 fs.writeFileSync(path.join(dir,'review.html'),shell('Lessons and review',`<h1>C++ &amp; Engineering Computing</h1><p class="lead">Your existing course setup, brought into Tesselate.</p><div class="notice"><strong>18 lessons · 9 labs · 58 reference programs.</strong> Follow the commented tutorials, explore source files in Code Desk, and check your understanding with 36 practice questions. Edit and run C++ programs in the online Code Desk without installing anything. MATLAB assignments require MATLAB.</div><div class="actions"><a class="button primary" href="desk.html">Open Code Desk →</a><a class="button" href="quiz.html">Practise</a><a class="button" href="setup.html">Local setup &amp; download</a></div><div class="grid">${lessons.map(l=>`<a class="lesson-card" href="${l.slug}.html"><span class="eyebrow">LESSON</span><h2>${escape(l.title)}</h2><p>${escape(l.summary)}</p><span>Read lesson →</span></a>`).join('')}</div><section><h2>Learn and code online</h2><p>Choose a tutorial, lab, or reference program in Code Desk. Edit its files, enter input, and select Compile &amp; run. Your edits save in this browser. Use the scratchpad for your own C++ program.</p><h3>Optional desktop tools</h3><p>The downloadable workspace includes the lab starters, tutorial code, reference examples, CMake build, VS Code build/debug tasks, and the local Code Desk with live input, console output, and file previews. Starter TODOs remain visible so you can complete your coursework.</p><a href="labs.html">Explore the nine labs →</a></section>`));
 const labs=programs.filter(p=>p.kind==='labs');
 fs.writeFileSync(path.join(dir,'labs.html'),shell('Lab guide',`<h1>Your nine course labs</h1><p class="lead">Read the task, find the starter files, and work through the implementation.</p>${labs.map(p=>`<section><p class="eyebrow">${escape(p.label)}${p.runnable?' · C++':' · MATLAB'}</p><h2>${escape(p.title)}</h2><div class="prose">${markdown(read(p.readme),path.posix.dirname(p.readme))}</div><a class="button" href="desk.html#${encodeURIComponent(p.id)}">Browse this lab’s files →</a></section>`).join('')}`));
 fs.writeFileSync(path.join(dir,'setup.html'),shell('Local setup',`<h1>Optional local setup</h1><p class="lead">You can now edit and run C++ directly on the website.</p><p><a class="button primary" href="desk.html">Open the online compiler</a></p><p>The steps below are only needed if you want desktop debugging or the original live console.</p><div class="actions"><a class="button primary" href="COMP139E-workspace.zip" download>Download the course workspace</a><a class="button" href="http://127.0.0.1:13900" target="_blank" rel="noopener">Open local Code Desk ↗</a></div><section><h2>Open the workspace</h2><p>Use your existing <code>Template/COMP139E</code> folder, or extract the download and open the extracted <code>COMP139E</code> folder in VS Code. The download includes sources and configuration; build it on your computer to create the executables.</p><p>Requirements: Python 3.10 or later, Visual Studio C++ Build Tools with CMake, and the Microsoft C/C++ extension for VS Code debugging. MATLAB is needed for the original MATLAB assignments.</p></section><section><h2>Build and launch</h2><p>In VS Code, press <strong>Ctrl+Shift+B</strong> to build. Then choose <strong>Terminal → Run Task → Open course dashboard</strong>. Alternatively, run these commands in a PowerShell terminal opened in the COMP139E folder:</p>${code('powershell -NoProfile -ExecutionPolicy Bypass -File scripts/build.ps1\npowershell -NoProfile -ExecutionPolicy Bypass -File scripts/dashboard.ps1')}<p>Keep that terminal open, then visit <a href="http://127.0.0.1:13900" target="_blank" rel="noopener">http://127.0.0.1:13900</a>. If the page cannot connect, start the dashboard first. It runs on the computer where you started it.</p></section><section><h2>Use the live console</h2><ol><li>Choose a lab, tutorial, or reference example.</li><li>Select <strong>Run program</strong>. Use example input where available.</li><li>Send keyboard input, finish input with EOF, or stop a running program.</li><li>Edit the source in VS Code, then select <strong>Build programs</strong> to run the updated code.</li><li>Read related headers and generated files beside the console.</li></ol><p>The online Code Desk compiles C++ through Judge0 and accepts input before each run. The optional local dashboard also supports a live interactive console and desktop debugging.</p></section><section><h2>Debug or use the terminal</h2>${code('.\\build\\bin\\main.exe tutorial 5\n.\\build\\bin\\main.exe example TestCircleWithHeader\n.\\build\\bin\\main.exe 2')}<p>With no command-line selector, <code>ACTIVE_EXAMPLE</code> takes priority, followed by <code>ACTIVE_TUTORIAL</code>, then <code>ACTIVE_LAB</code> in <code>main.cpp</code>. Press F5 to use the supplied debugger configuration.</p></section><section><h2>MATLAB and unfinished labs</h2><p>Labs 8–9 require MATLAB. Tutorial 16 and 17 provide C++ introductions, and <code>tutorials/matlab</code> contains plotting and integration practice. The lab starters intentionally contain TODOs; a TODO result identifies work left to implement.</p></section>`));
 for(const mode of ['quiz','exam']){
  const exam=mode==='exam';fs.writeFileSync(path.join(dir,mode+'.html'),shell(exam?'Practice test':'Practice quiz',`<h1>${exam?'COMP 139E practice test':'Practise C++ and computing'}</h1><p class="lead">${exam?'18 questions · 45 minutes · solutions after submission':'Trace the code, choose an answer, and read the explanation.'}</p><section id="setup"><label for="topic">Topic</label><select id="topic"><option value="all">All course topics</option>${lessons.map((l,i)=>`<option value="${i}">${escape(l.title)}</option>`).join('')}</select><p>There are two questions per lesson. All topics samples one from each of the 18 lessons. ${exam?'The timer starts when you begin; unanswered questions count as incorrect.':'Check answers as you go and follow the lesson links to revisit a concept.'} These are original practice questions, not an official exam.</p><button id="start" class="primary">${exam?'Start test':'Start quiz'}</button><button id="resume" hidden>Resume saved attempt</button></section><p id="storage-note" role="status"></p><section id="session" hidden></section><section id="results" hidden tabindex="-1"></section><noscript><a href="review.html">Read the static lessons and code examples.</a></noscript>`,`<script defer src="../assets/vendor/katex/katex.min.js"></script><script defer src="../assets/study/math.js"></script><script defer src="../assets/study/code.js"></script><script defer src="bank.js"></script><script defer src="../engr290/practice.js?v=comp139e-1"></script>`));
 }
 fs.writeFileSync(path.join(dir,'desk.html'),shell('Online Code Desk',require('./comp139e-desk.cjs'),'<script defer src="compiler.js?v=1"></script><script defer src="desk.js?v=online-1"></script>',true));
 for(const q of bank.questions){format(q.prompt);q.options.forEach(format);format(q.explanation);}
 fs.writeFileSync(path.join(dir,'bank.js'),'window.STUDY_COURSE = '+JSON.stringify(bank)+';\n');
 fs.writeFileSync(path.join(dir,'lessons.json'),JSON.stringify(lessons.map(({slug,title,summary,sources})=>({slug,title,summary,sources})),null,2)+'\n');
 // Allow a hosted Code Desk link to select a program in the downloaded local dashboard.
 const localApp=path.join(workspace,'dashboard/app.js');let app=fs.readFileSync(localApp,'utf8');
 if(!app.includes('searchParams.get("program")')){app=app.replace('selectProgram(programs.find(p => p.id === saved)', 'const requested = new URL(location.href).searchParams.get("program");\n    selectProgram(programs.find(p => p.id === requested) || programs.find(p => p.id === saved)');fs.writeFileSync(localApp,app);}
 console.log('Built 18 lessons, 9-lab guide, Code Desk, local setup, quiz, and practice test.');
})().catch(error=>{console.error(error);process.exitCode=1;});
